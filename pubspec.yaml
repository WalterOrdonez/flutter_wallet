name: bam_wallet_transfers
description: 'A new Flutter project.'
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  dio: ^5.9.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  flutter_svg: ^2.2.3
  freezed_annotation: ^2.4.4
  go_router: ^14.8.1
  intl: any
  shared_preferences: ^2.3.4

dev_dependencies:
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  freezed: ^2.5.7

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
    - env_dev.json
    - env_prod.json
